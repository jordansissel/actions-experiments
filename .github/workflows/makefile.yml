name: Makefile CI

on:
  workflow_dispatch:
    inputs:
      run:
        default: echo ok

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: env
      env: 
        inputs: ${{ toJSON(inputs.*) }}
        inpuys2: ${{ toJSON(inputs) }}
